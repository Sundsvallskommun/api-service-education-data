#========================================
# Test profile settings (junit tests)
#========================================
logging:
  level:
    root: INFO
spring:
  datasource:
    url: jdbc:tc:mariadb:10.6:///
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate

scheduler:
  import:
    cron: "-"
    name: "scheduled-import"
    shedlock-lock-at-most-for: "PT15M"
    maximum-execution-time: "PT15M"
    json-size: 100

integration:
  planned-education:
    base-url: http://localhost/planned-educations
  susa-navet:
    base-url: http://localhost/susa-navet/emil3